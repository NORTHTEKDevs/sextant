[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lemmas"
version = "0.4.0"
description = "Seven reliability primitives for any LLM API: Chain-of-Verification, self-consistency, best-of-N, reflexion, debate, drift detection, hedged execution."
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Kristian Baer", email = "info@northtek.io"},
]
requires-python = ">=3.10"
keywords = [
    "llm", "ai", "reliability", "chain-of-verification", "cove",
    "self-consistency", "drift-detection", "hedged-execution",
    "openai", "anthropic",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.24",
]

[project.optional-dependencies]
openai = ["openai>=1.0"]
anthropic = ["anthropic>=0.30"]
gemini = ["google-generativeai>=0.7"]
groq = ["groq>=0.10"]
all = ["openai>=1.0", "anthropic>=0.30",
        "google-generativeai>=0.7", "groq>=0.10"]
dev = [
    "pytest>=7",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
]

[project.urls]
Homepage = "https://github.com/NORTHTEKDevs/lemmas"
Source = "https://github.com/NORTHTEKDevs/lemmas"
Issues = "https://github.com/NORTHTEKDevs/lemmas/issues"

[tool.setuptools.packages.find]
include = ["lemmas*"]
exclude = ["tests*", "examples*"]

[tool.setuptools.package-data]
lemmas = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # line-too-long is fine; we check by eye

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
asyncio_mode = "auto"
